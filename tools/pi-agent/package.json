{
  "name": "pi-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./extension.ts"]
  },
  "scripts": {
    "check": "node --check daemon.mjs && node --check protocol.mjs && node --check security.mjs && tsc --noEmit && node --test tests/daemon.test.mjs"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@earendil-works/pi-tui": "0.85.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}

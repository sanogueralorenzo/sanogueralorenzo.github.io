[package]
name = "voice_engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
jni = "0.21"
once_cell = "1.21"
regex = "1.12"

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

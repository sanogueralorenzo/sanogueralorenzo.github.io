package fixtures.invalid_unsupported_step_statement

type Report = Result

goal reject_unsupported_step_statement() -> Report {
  context repo("./")

  plan {
    step summarize -> Report {
      delegate reviewer
    }
  }

  verify {
    require no_policy_violations
  }
}

package fixtures.invalid_capability_grant_argument_type

type Report

goal invalid_capability_grant_argument_type() -> Report {
  capability shell {
    run command: 30
  }
  plan {
    step run_tests -> Report {
      effect ShellExec(command: "npm test")
    }
  }
  verify {
    require no_policy_violations
  }
}

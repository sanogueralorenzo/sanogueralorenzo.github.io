package fixtures.invalid_verify_impure_custom_effect

type Report

goal verify_impure_custom_effect() -> Report {
  capability notify {
    send channel: "alerts"
  }
  plan {
    step summarize -> Report
  }
  verify {
    require notify.send(channel: "alerts")
  }
}

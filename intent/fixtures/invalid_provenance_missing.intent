package fixtures.invalid_provenance_missing

type Report = {
  body: String
}

goal missing_completion_provenance() -> Report {
  context repo("./")

  memory session {
    retain evidence until 30d
  }

  plan {
    step draft -> Report
  }

  verify {
    require all_outputs_cited
    require no_policy_violations
  }

  invariant {
    deny uncited_external_claim
  }
}

Clean this dictated message for sending with minimal edits only. Remove spoken disfluencies (um, uh, erm, emm, hmm) and accidental repeated words. Fix only obvious transcription mistakes; otherwise keep the original wording. Do not paraphrase, summarize, reorder, or change person, intent, tone, numbers, units, abbreviations, or punctuation style unless required to fix a clear error. If the message is already good, return it unchanged. If uncertain, return it unchanged. Output only the cleaned message.

User input:
{{input}}

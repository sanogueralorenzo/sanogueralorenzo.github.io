TELEGRAM_BOT_TOKEN=123456:replace-with-your-bot-token
# Optional allowlist. Comma-separated Telegram chat IDs.
# If omitted, all chats can interact with the bot.
TELEGRAM_ALLOWED_CHAT_IDS=1234567890

# Codex auth. If omitted, Codex can use your existing CLI login/session.
# Any non-empty API key string.
# CODEX_API_KEY=sk-...

# Any valid OpenAI-compatible base URL.
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional. Defaults to $HOME/.codex if omitted.
# Supports: ~, $HOME, ${HOME}, absolute path.
# CODEX_HOME=$HOME/.codex

# Where Codex should run commands/files.
# Optional. Defaults to $HOME if omitted.
# Supports: ~, $HOME, ${HOME}, absolute path.
# CODEX_WORKING_DIRECTORY=$HOME
# true | false. Default is true when omitted.
CODEX_SKIP_GIT_REPO_CHECK=true

# Optional Codex runtime behavior.
# Any model id supported by your Codex setup.
# CODEX_MODEL=gpt-5-codex
#
# Access mode presets (pick one set):
#
# 1) Read-only (safest)
# CODEX_APPROVAL_POLICY=on-request
# CODEX_SANDBOX_MODE=read-only
# CODEX_NETWORK_ACCESS_ENABLED=false
#
# 2) Workspace write (recommended middle ground)
# CODEX_APPROVAL_POLICY=on-request
# CODEX_SANDBOX_MODE=workspace-write
# CODEX_NETWORK_ACCESS_ENABLED=true
#
# 3) Full access (most permissive)
# CODEX_APPROVAL_POLICY=never
# CODEX_SANDBOX_MODE=danger-full-access
# CODEX_NETWORK_ACCESS_ENABLED=true
#
# Allowed values:
# CODEX_APPROVAL_POLICY: untrusted | on-request | on-failure | never
# CODEX_SANDBOX_MODE: read-only | workspace-write | danger-full-access
# CODEX_NETWORK_ACCESS_ENABLED: false | true

# Make bot-created sessions show in the default Codex app conversation list.
# Session source options:
# - vscode   -> recommended originator: Codex Desktop (best for Codex Mac app default list)
# - cli      -> recommended originator: Codex CLI
# - appServer-> recommended originator: Codex App Server
CODEX_FORCE_SESSION_SOURCE=vscode
# Any non-empty label string. Default is "Codex Desktop".
CODEX_FORCE_ORIGINATOR=Codex Desktop

# Local storage for Telegram chat -> Codex thread_id map
# Supports: ~, $HOME, ${HOME}, absolute path, or relative path.
BINDINGS_FILE=runtime/bindings.json

# Voice-message transcription (local/on-device) is always enabled.
# The bot always uses ./scripts/transcribe-whispercpp.sh internally.
# Required by scripts/transcribe-whispercpp.sh (tiny-only):
# WHISPER_MODEL_PATH_TINY=$HOME/AndroidStudioProjects/sanogueralorenzo.github.io/agents/gateway/models/ggml-tiny.en.bin

# true | false. When true, bot echoes transcript text back before sending to Codex.
# Default is false.
# TELEGRAM_VOICE_ECHO_TRANSCRIPT=false

# Fallback decision when approval prompt can't be delivered or times out.
# Allowed: accept | acceptForSession | decline | cancel
# Default: decline
# TELEGRAM_APPROVAL_DEFAULT_DECISION=decline

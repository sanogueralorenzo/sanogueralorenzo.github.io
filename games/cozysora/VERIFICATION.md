# Cozy Sora runtime verification

Verified on 4 September 2026 with Godot 4.7.2, Forward+, Metal, Apple M3 Max. No automated tests were created. Verification used native play, rendered captures, source review, and engine logs.

## Original destination integration

The game starts on Cozy Sora destination selection without generating the map. That integration originally registered Seabreeze Village alone; Harbor Hills is covered below. Its preview is a capture of the playable procedural coast. Final layouts were inspected at 320 × 640, 390 × 844, 844 × 390, 1280 × 720, and 1920 × 1080. Narrow and short layouts retain accessible Settings and Play actions; larger layouts use a landscape preview beside the map description. Short screens omit secondary copy where needed, and content can scroll.

Native interaction covered keyboard selection and focus outlines, mouse buttons, settings focus cycling, volume and mute changes, restoring settings, map loading, pause/resume, cat/seagull selection, returning, and repeated re-entry. An independent reviewer completed two full entry/return cycles; the primary review completed additional cycles. Each playing session reported one player, one camera, and one audio player. Every observed return reported zero players, cameras, audio players, and orphan nodes. Settings were restored to 100% sound, unmuted, with manual touch controls disabled.

Touch controls were exercised through their native mouse interaction path: movement pad, right-side look, mode switch, pause, and return. The final seeded map was also played after the foliage correction: the cat walked clear of the starting foliage, and the seagull climbed and flew toward the coast. Real touch events ignore emulated mouse duplicates. The 320-pixel layout keeps the pad and action hit areas separate. Controller bindings and menu focus paths were inspected in code.

## Preserving Seabreeze Village

Before/after runtime captures covered the normal cat camera and ten scenic views: coast, paddy, farm, rail, village, alley, vending, viaduct, shrine, and top. An independent critic initially rejected the comparison because foliage placements changed when the cache regenerated.

Fresh runs of the original code also produced different foliage. Two helper methods in the custom seeded generator called bare `randf()`, which Godot resolves to its global random function. Qualifying those two calls as `self.randf()` fixes the preexisting nondeterminism. This is the only intentional change to the map's generation algorithm; authored terrain, roads, landmarks, buildings, props, palette, atmosphere, and placement rules remain unchanged.

The original application and the refactored application were then freshly rendered with that identical two-line correction. Both generated 833 trees and shrubs. The independent critic inspected all eleven paired views and confirmed matching foliage placements, canopy shapes, roads, fields, architecture, props, camera composition, materials, and fog. Remaining frame differences were consistent with shader wind, water/cloud timing, and particles. Older randomly generated caches can have different canopies; the corrected seed now gives a repeatable world. The selector preview was refreshed from this final coast.

## Performance and review

The immediate pre-refactor warm capture loaded in 13.1 seconds during concurrent workstation load. Refactored native review entries took 13.3–14.4 seconds under contention and 6.1 seconds in the final run. Fresh corrected generation took 14.3 seconds in the original application and 15.6 seconds in the refactored application, including its transition and frame yields. The final native gameplay usually held 120 FPS with roughly 0.4–0.7 ms physics processing. Capture and heavily contended runs varied substantially; these observations are runtime checks, not controlled benchmarks.

Independent reviews approved the landing design, ownership boundaries, repeated scene lifecycle, and final paired village views after fixing focus handling, settings isolation, invalid scene handling, narrow layouts, and the seeded-generator bug. Final runtime and import logs contained no script errors or crashes.

## Platform limits

Physical controller hardware, genuine simultaneous multi-finger input, and audible output were not independently verified. Native input handling, settings, synthesized audio lifecycle, pause, and teardown were exercised or inspected. The responsive UI was rendered at mobile and desktop sizes; no browser or mobile export was built. Forward+ remains the intended gameplay renderer and does not support Godot's web export. A browser release would require a separate renderer/export compatibility pass. Captured-mouse look remains difficult to assess through native UI automation; its original camera math is retained, and drag-look was exercised with touch controls enabled.

All bundled previews, shaders, meshes, textures, and sounds are project-owned or generated by the project. No external game assets, fonts, models, animations, audio, or map data were added.

## Harbor Hills integration

The second destination registers `harbor_hills` with a native `cable_car` preview, original description, normal café-side cat spawn, dedicated scene and separate generated cache. Its static build contains 252 cypress and 238,455 grass clumps in 78 spatial batches. All new content is procedural and map-owned. No automated tests or external assets were added.

Repeated independent visual review covered normal spawn, waterfront, commercial frontage, residences, park, cable car, stairs, courtyard, roofs, flight and top view. Corrections included terrain-draped street edges, connected sidewalk crossings, level furnished courts, readable shop signs, richer displays and cable-car detail, leaf-card foliage, occupied roof/rear elevations, grounded distant scenery, and stair geometry that shares its landing elevation. The final eleven-view Harbor set received independent visual acceptance, including a final review of corrected ramp joins and foundations that reach the lowest terrain corner. The independent gameplay critic also gave final acceptance after rechecking every identified traversal blocker.

All eleven Seabreeze views were rendered before and after this integration. The independent critic accepted preservation of terrain, road layout, buildings and props, foliage placement, character/camera framing, palette and atmosphere. Remaining pixel differences were consistent with moving wind, water and shadows. No Seabreeze generator or shader changed. Its warm capture entry took 6,111 ms, compared with 6,244 ms for the pre-integration capture on this workstation.

Native walking, jumping, flight and ground perching were exercised through keyboard and touch-control pointer input. The first gameplay review caught floating cross-street ribbons; those now sample terrain at both edges. Follow-up play exposed the small curb and stair entrance joins that scenic captures alone did not establish; dedicated lowered crossing approaches, a stair lead-in and removal of the overlapping sidewalk resolve those interfaces. The independent final stair check walked from Z = 45 through 62, 78 and 91, then back down through 79, 61 and 45 to 30, remaining grounded without jumping. The pavilion ramp was also traversed to its roof, and cat-to-gull switching preserved that elevated position. A focused roof check landed the gull at Y = 55.568 m and returned the cat to the same roof at Y = 55.398 m. The workshop's rising ramp and flat bridge were walked continuously onto its roof after aligning their join. Under a solid shop canopy, upward gull motion stopped below the awning and switching returned the cat to the forecourt. At the bay, the cat stopped at Z = −116.486 m; the gull settled at the water surface (Y = −0.78 m), and switching recovered the last safe promenade position. New Harbor-only support queries and capsule clearance checks preserve roof height, guard overhead switching and recover unsafe sea positions. Seabreeze continues using its existing controller branch.

The final independent Harbor → selector → Seabreeze → selector → Harbor → selector sequence passed. Harbor warm re-entry took 840 ms and restored its normal spawn from its own cache. The sequence reported one player, one camera, two audio players in Harbor and one audio player in Seabreeze. Every return reported zero active sessions, players, cameras, audio players and orphan nodes. The release run observed a complete 160-second cable-car round trip: Z = −60.33 at 20 seconds, −16.95 at 40, 43.05 at 60, the hilltop at 96 with 180° heading at 80, then 66.80, 30.95 and −29 on the return, and the waterfront at −100 with its original heading at 160. Bell triggers continued throughout, and pause stopped the simulation clock. The spatial cable-car bell belongs to the map and pauses with its simulation; the shared ambient stream also pauses. Profiling now records viewport size, character position/mode/grounding and bus output level, making native traversal and pause observations inspectable. A paused sample read −200 dB; active Harbor ambience typically read around −30 to −35 dB.

The final cold Harbor capture build took 4,874 ms. Warm focused entry took 744 ms. After spatial grass batching, the corrected stair run reported 112–145 FPS at 1280 × 720 with physics usually around 0.6–0.8 ms, with occasional higher samples. A later complete transit/lifecycle run held 60 FPS in both destinations; its Harbor cold entry took 6,663 ms and Seabreeze warm entry 11,491 ms under the contemporaneous workstation load. Earlier unbatched and contended walkthroughs varied more widely; enlarged Retina windows also increased render cost substantially in both maps. These are observed native runs on one workstation, not controlled performance benchmarks.

The two-card selector was rendered at 1280 × 720, 390 × 844 and 844 × 390, with the welcome header and Settings visible on entry, accessible Play controls, and scrolling/focus navigation to Harbor. A layout-settling scroll fix prevents initial focus from hiding the header; it respects visibility, loading state and a changed focus owner.

Final evidence was captured under `/tmp/harbor-hills/release`, with paired Seabreeze captures under `/tmp/harbor-hills/seabreeze-before` and `/tmp/harbor-hills/seabreeze-after`. The project bundles only its selector preview; the README capture commands reproduce the inspection views. Controller hardware and physical multi-touch remain the platform limits described above.

## Harbor Hills quality pass · 5 September 2026

Harbor Hills was compared with Seabreeze Village's normal cat view, inhabited village and coast, and with its own eleven original viewpoints. Five substantive visual iterations addressed sparse street edges, disc-like foliage, bare park turf, unfinished terrain transitions, shop forecourts, garden seating and simple boats. Two independent critics accepted the fifth iteration at the first map's demonstrated visual standard. A subsequent final review accepted the 110 m grass visibility limit and confirmed preservation of Seabreeze's start, coast and village captures. Their acceptance concerns visuals; native runtime verification was performed separately.

The pass adds generated leaf-spray textures, broader tree crowns, curved grass with the shared grass shader, painted surface variation, steep-cut stone shading, planted curb beds, bicycles, stoop details, shop displays, communal garden tables, a furnished waterfront, shaped hulls and rigging, wind-driven laundry, and sparse GPU garden seeds. All content is code-authored or generated. No external assets or automated tests were added. Harbor's normal spawn and map title behavior are preserved.

Native keyboard play covered walking and sprinting from the café, curb jumping, terrace access, the complete stair street in both directions, cat/seagull switching, climbing, flight, ground settling, pause and return to destinations. Close traversal found a reproducible capsule contact trap beside a café wall: input was present, but all six slide contacts repeated the same sloped-ground normal without displacement. A Harbor-only 0.015 m recovery margin resolved this. Re-entry at the same position could back away, and the cat subsequently walked from the sidewalk over the café ramp to its forecourt. Stair traversal went from Z = 45 through 53.72 to 97.58 and back down to 26.25 without jumping, remaining grounded. Flight climbed above the neighborhood and settled in the park. Paused character positions remained fixed and audio read −200 dB. Return to selection reported zero active sessions, players, cameras, audio players and orphan nodes. The cable car completed its existing 160-second circuit during the run.

Temporary spawn changes and contact logging used for the focused native walkthrough were removed. The sole shared controller change is the recovery margin for maps opting into surface traversal; Seabreeze does not use that branch. No Seabreeze generator, shader or map data changed. Both destinations were rendered again after the final code changes. Engine import and final runtime logs contained no script errors or crashes.

Final cold generation took 7,463 ms and the final warm entry took 778 ms on the same M3 Max/Metal setup. Native play commonly reported 50–120 FPS at 1280 × 720, with physics generally around 0.5–1.0 ms. During computer-use capture and workstation contention, observed rates varied down to single digits; enlarged Retina windows also changed rendering cost. These are observations from interactive verification, not a controlled performance benchmark. Physical controller and multi-touch limitations remain as recorded above.

Evidence for this pass is under `/tmp/harbor-polish`: `before`, `reference`, five review rounds, `final`, `reference-after`, and native play logs. The bundled Harbor preview is a new native 1280 × 720 cable-car view. The README commands reproduce the scenic captures.

## Rounded Harbor street ends — 5 September 2026

Replaced all eleven square outer road cutoffs with semicircular asphalt, curbs and continuous sidewalks. The polar mesh samples the hillside with shared vertices; tree, grass, shrub and flower placement avoids the new paved footprint while retaining the seeded planting sequence elsewhere. Top and ground-level `street_end` native captures confirmed closed joins and clear paving. A native cat walkthrough at the uphill X = 90 m street crossed the straight/rounded asphalt join, jumped the raised curb to the grass, and returned across the sidewalk to the road. Profiling recorded grounded positions on the curved sidewalk and asphalt without errors. Cold generation took 8,244 ms; the walkthrough generally held 118–120 FPS at 1280 × 720. No tests or external assets were added.

## Shared procedural component refactor — 5 September 2026

Reviewed both maps' geometry, materials, generated textures, vegetation, props, batching, collision and cache ownership. Shared components now construct primitive meshes and collision shapes, build spatial MultiMeshes, merge static architecture, cache toon materials, rasterize leaf silhouettes, install atmosphere and drifting particles, and manage generated-scene signatures/ownership. Seabreeze's generators and terrain/rice shaders moved into its map folder; plant mesh construction, texture recipes and building finishes were separated from placement code. Unique terrain, grass topology, canopy recipes, props, palettes and RNG algorithms remain local. Shared player changes only replace duplicate material/primitive construction; movement, camera, input and audio algorithms are retained.

Baseline source was archived from `240764e3e`. Native 1280 × 720 captures covered all 23 views: normal spawn plus eleven Harbor scenic views, and normal spawn plus ten Seabreeze views. The complete sets were rendered again after the final changes and reviewed side by side. Layout, architecture, ground, road ends, planted positions, materials, character framing and atmosphere were preserved. Image differences follow wind, cloud/water animation, particles and vehicle timing; the cable car occupies a different point along its unchanged route in successive captures. Fresh generation retained Seabreeze's 833 trees/shrubs and Harbor's 288 trees and 295,676 grass clumps in 83 grass batches.

The first runtime pass caught and corrected a leaf-shading inference error and a stale chain-link material-cache reference. Threaded scene loading also produced four or five zero-reference `RefCounted` shutdown leaks with the extracted resource dependencies. A controlled native comparison retained the same cached geometry and removed the leaks by loading the small generator scene on the main thread. That approach is now used centrally; expensive generation still yields between stages. Final native capture logs and the complete gameplay-session shutdown contain no script errors, leak warnings or crashes.

Native play completed Harbor → selector → Seabreeze → selector → Harbor → selector → Seabreeze → selector. Walking, jumping, shared cat/seagull switching, climbing/descending in flight, Harbor ground perching, pause and return were exercised. Every session contained exactly one player and one camera, with two audio players in Harbor and one in Seabreeze. Every return recorded zero active sessions, players, cameras, audio players and orphan nodes. Paused positions stayed fixed and audio reached −200 dB. Both re-entries restored the registered normal cat spawn; Seabreeze explicitly restored its generated foliage cache. The native process exited cleanly after the final return.

Warm final captures entered Harbor in 792 ms and Seabreeze in 6,083 ms, compared with 788 ms and 6,260 ms in the baseline captures. Refactored cold generation was observed at 8,207 ms for Harbor and 20,641–23,364 ms for Seabreeze. Interactive cached re-entry took 759 ms and 8,156 ms respectively. Harbor gameplay commonly held 117–120 FPS; Seabreeze's final cached spawn held about 99–100 FPS. Contended Seabreeze periods during native UI automation dropped to 34–46 FPS, with increased physics time; these workstation observations are not a controlled benchmark. Spatial cell sizes, draw distances, shadow policy and mesh tessellation are preserved.

Cache signatures were inspected to include the selected map's generation sources and profiles, all shared component sources and common shaders. Both maps regenerated after shared edits; the later Seabreeze-only profile edit rebuilt Seabreeze while Harbor reused its unchanged cache. Warm entries restored the new signatures correctly. No obsolete generator paths remain, and the moved shader/scene UIDs are retained. Documentation in `shared/README.md`, both map folders, `MAPS.md` and the project README explains shared versus local changes and verification. No tests, external assets or dependency packages were added. Physical controller and simultaneous multi-touch limitations remain as described above.

Evidence is under `/tmp/cozysora-shared`: `baseline`, `before`, `final`, six final comparison sheets, final capture logs and `play-final.log`. Captures can be reproduced with the existing README commands.

## Daan Gardens — native verification, 5 September 2026

Built and inspected with Godot 4.7.2, Forward+ / Metal on Apple M3 Max at 1280 × 720. The final procedural build reported 285 trees and 222,703 grass clumps in 99 spatial batches; a cold final generation took 2,086 ms. Native play generally held the active window's 60 or 120 FPS cadence, with sampled physics times usually below 2 ms. The full final capture pass completed without script, shader or runtime errors. No tests or external visual/audio assets were added.

Native keyboard play covered the entrance paving and jump, pond deck ramp and railing, dry-bank movement limits, gull water settling and recovery when switching to the cat, flight above the pond/crowns, grove landing, café doorway/counter/ceiling, pavilion access and bench collision, and roof perching. The cat crossed the deck at Y ≈ 1.33 m and stopped on the dry pond bank at (0.776, 0.799, −11.908). A gull perched on an apartment roof at Y = 16.23 m, and paused-menu switching returned the cat to the roof surface. Continued flight input held the north limit at Z = −111 m and the height limit at Y = 95.35 m. Targeted locations were staged through temporary spawn metadata for native checks; the delivered spawn is the entrance at (59, ground + 0.05, −58).

The roof walkthrough exposed a shared pause bug: disabling the gameplay session removed physics bodies, making paused-menu surface queries fail. The application now marks session collision bodies `DISABLE_MODE_MAKE_STATIC` after construction. Geometry remains frozen and queryable while processing/audio pause, and original physics modes return on resume. Temporary diagnostic logging was removed. The roof and pavilion menu-switch checks were repeated successfully with this change.

Independent critics compared repeated native capture sets with both existing maps. Corrections included continuous surrounding city ground, complete distant tree crowns, smoother lawn material, varied tree branching and understory, an irregular planted bird island, neighborhood courtyards and scooter clusters, calm pond reflections, correctly oriented pavement/tiles, a visible pavilion roof underside, restrained woodgrain, and clear deck/ramp planting. Both critics approved the final visual set. Final normal cat captures confirm that the deck approach is free of grass penetration and the café/pavilion spaces remain usable.

The final native session completed Seabreeze → Harbor → Daan → Seabreeze → Harbor → Daan, returning to the selector between every destination. All six entries restored the registered normal spawn and exactly one player/camera, with two audio players in Harbor and one in the other maps. Every return reported zero active sessions, players, cameras, audio players and orphan nodes. Both cycles exercised movement, jumping, character switching, flight and pause/resume. The final cached entries took 7,156 ms for Seabreeze, 735 ms for Harbor and 464 ms for Daan. The process exited cleanly after the final return, with no script errors, leak warnings or crashes.

Harbor regression play approached the cable car at its lower stop, made contact during a jump, paused mid-jump and resumed safely onto the street. The car's route clock, character position and audio froze while paused and resumed correctly. This establishes contact and pause/resume behavior, not sustained riding. During the earlier extended transit wait, rendering temporarily fell to roughly 8–15 FPS, including while paused, then recovered without a code or process change. Paused physics remained roughly 0.07–0.4 ms during that interval. The cause remains unexplained; the final Harbor reload sampled about 110 FPS and Daan 120 FPS. These are native workstation observations, not a controlled benchmark or a claim of uninterrupted frame-rate stability.

Both independent critics inspected the final reload log and native screenshots and granted final visual, gameplay and regression acceptance, with the transient rendering slowdown retained as a verification limitation. Final scenic evidence is under `/tmp/daan-final`, with native interaction captures and focused play logs under `/tmp/daan-runtime` and `/tmp/daan-*-play.log`. The complete reload record is `/tmp/daan-lifecycle-final.log`. The bundled preview is the map's own native pond capture. Physical controller and simultaneous multi-touch limitations remain as described above.

## Shared summer polish — 5 September 2026

Compared all three maps with the supplied summer-light reference and matched native captures from `e9804f3a2`. Layouts, landmarks, registered spawns, character geometry, controls and global 60 FPS cap are preserved. No automated tests or external assets were added.

Shared changes give foliage correct normals under nonuniform scaling, warmer sun with neutral readable shade, restrained contact occlusion, quieter painterly filtering, and derivative-filtered procedural surface variation. Two blended shadow cascades use medium PCF filtering and FXAA. Independent review rejected an intermediate contact-distance shadow pass because it left gritty stippling on Seabreeze pavement and the cat. The delivered filtered shadows remove that grain while retaining leaf breakup and grounded feet/props. A separate Harbor paving interference issue was traced to shadow bias and corrected at the light rather than hidden in road materials.

Seabreeze retains its seeded tree/bush centers and 833-tree/shrub population. Finer generated leaf silhouettes, tapered grass blades and directional roadside pruning expose the route while preserving long planted verges. Harbor's existing cypress crowns taper and vary vertically, street trees have warmer tapered forks, and exterior stairs gain stringers, handrails and brackets. Daan's existing trees have warmer bent, tapered branches with buried joints; the pond receives curved reed leaves and low fern layers clear of paths. New wood geometry in Harbor and Daan has matching capsule collision segments. Grass placement counts and spatial batching remain intact.

A conspicuous white polygon in Seabreeze's ocean was ray-traced to distant terrain at approximately (136, −23, −135). The old fixed coastal subtraction left inland hill contributions protruding above the −30 m sea. The height field now converges to the −34 m seabed along the existing coastal transition. Equivalent coast renders confirm a continuous ocean. Water highlights also use smooth, restrained blue-white ripples instead of a binary specular threshold.

Native contact checks passed at Daan's banyan court and Harbor's street trees: the cat stopped against wood, jumped and deflected around roots, the gull crossed and climbed clear of crowns, and the camera shortened against nearby trunks without trapping the view. Paused-menu character switching retained a safe supported position. The initial Seabreeze entry/return also reported a clean selector with no remaining player, camera, audio or orphan nodes.

An intermediate four-cascade shadow configuration developed severe frame-time instability during extended native play, including paused views. Comparison runs at the same paused Harbor pose retained the baseline’s steady frame times and reproduced the slowdown in the polished version. Live SSAO, FXAA and screen-filter toggles did not resolve it; disabling directional shadows restored steady frame times. Reducing the shadow setup to two blended cascades retained the accepted filtering and contact shading while reducing shadow passes. The exact paused view was replayed for over 90 seconds, followed by the final matched captures. These observations identify the shadow configuration as the practical fix, without claiming a proven Metal driver defect.

Earlier measurements with a stale second Godot play session were excluded from the final comparison. Later checks used a case-insensitive process inventory to confirm exactly one Godot instance. The global 60 FPS cap and original VSync setting remain unchanged. All temporary render toggles and paused-start controls were removed.

Both independent reviewers assessed the polish against baseline captures and the supplied reference. The visual critic approved all nine final normal cat/gull poses and twelve final scenic spot-checks across the three maps, after the complete 39-view scenic set rendered successfully. Contact shadows, leaf breakup, quiet paving, architecture readability and clear routes were retained with two cascades; no visible split seam or substantive quality loss was found. The three bundled previews are the final native coast, cable-car and pond captures. Source review confirmed preserved route/landmark coordinates and no external art additions.

The final native run completed Seabreeze → Harbor → Daan twice, exercising movement, jumping, cat/gull switching, flight, pause/resume and return on each entry. Every return produced an empty selector: seven reports with zero sessions, players, cameras, audio players and orphan nodes. Re-entry restored each registered spawn. An external focus loss in the second Harbor entry auto-paused correctly; raising, resuming and repeating movement confirmed displacement before continuing. The final process exited with code 0 and no errors or leak warnings. The runtime critic explicitly accepted gameplay, lifecycle and observed rendering stability.

Final measurements used Godot 4.7.2, Forward+ / Metal, Apple M3 Max, 1280 × 720, exactly one native process, and ordinary play without capture options. Values below are second-entry warm loads and the ranges of five-second frame windows across those entries; percentiles are per window, not pooled. Baseline load values come from the matching two-cycle run.

| Map | Warm load baseline / final | Final frame median range | Final p95 range |
| --- | --- | --- | --- |
| Seabreeze Village | 12.570 / 12.071 s | 16.638–16.696 ms | 17.028–21.863 ms |
| Harbor Hills | 0.909 / 0.929 s | 16.612–16.799 ms | 18.121–21.189 ms |
| Daan Gardens | 0.555 / 0.612 s | 16.659–16.774 ms | 17.730–22.950 ms |

The earlier sustained 13–47 FPS slowdown did not recur. Quiet paused medians remained near 16.67 ms through the final Harbor and Daan views. Isolated outliers remain: Harbor recorded a 101.9 ms maximum frame and a 56 FPS counter sample; Daan recorded 47.7 ms and 57 FPS. These did not become sustained degradation. The effective cap remained 60 and VSync remained enabled. Metal GPU timing returned zero and is treated as unavailable. These are observations from one workstation, not a guarantee of uninterrupted 60 FPS on every machine; physical controller and simultaneous multi-touch limitations remain as previously recorded.

Evidence is under `/tmp/cozysora-summer`: `before` and `before-play` contain baseline views; `final-two` and `final-two-play` contain final scenic and normal views; `performance-clean` contains the isolated frame-time comparisons and shadow diagnosis; `runtime-baseline` and `runtime-two` contain the extended native cycles. Capture and pose commands are documented in the README. No script, shader or import errors occurred in the final 48 captures and preview import.


## C# / Godot .NET migration — 7 September 2026

Migrated all game scripts to C# targeting .NET 10 with Godot.NET.Sdk 4.7.2. Forward+ and its existing lighting, fog, shadow, post-processing and antialiasing settings are preserved. The three map generators, materials, procedural images, meshes, particles, characters, motion, collision queries, synthesized audio, menus, touch controls, resource profiles and generated-cache lifecycle are now owned by the corresponding C# classes. No GDScript runtime remains, and no tests or external game assets were added.

The desktop editor's hostfxr error was resolved by making the installed ARM64 .NET 10 SDK discoverable to GUI applications, shell sessions and Godot's build tool. The SDK lives outside the repository. The delivered launcher resolves a user SDK, checks for a .NET Godot build, builds from the canonical project path and launches the game or editor. A clean .NET build and Godot editor import/build both completed successfully.

Visual baseline source was archived from `4d8eb357a3605650845a7ae99a6322f833ef6994`. Native Forward+ captures compare the same 39 views: eleven Seabreeze, twelve Harbor and sixteen Daan views, including their normal cat cameras. An independent critic compared geometry, seeded planting, paths, roads, architecture, props, camera framing, palette and atmosphere. The review caught an alpha-texture migration error: transparent white padding brightened Seabreeze's chain-link wires under bilinear/mipmap filtering. Restoring the original transparent black padding corrected the fence. The final native captures include that correction. The independent critic accepted all 39 final views with no remaining substantive visual migration differences. Time-dependent wind, birds, clouds, water and moving vehicles naturally differ between sequential captures.

Native C# play covered all three destinations in a single session, walking/jumping, cat/seagull switching, gull climbing and automatic flight, collision with trees, safe recovery from water, pause/resume, keyboard menu navigation, touch-pad movement and character switching, mute and settings restoration. Each playing session had one player and camera, with two audio players in Harbor and one in the other maps. Every return to selection reported zero active sessions, players, cameras, audio players and orphan nodes. Settings were restored to full volume, unmuted, with manual touch disabled. Capture-only runs omit the player synthesizer by design.

Fresh generation preserved Seabreeze's 833 trees/shrubs, Harbor's 288 trees and 295,676 grass clumps in 83 batches, and Daan's 285 trees and 222,703 grass clumps in 99 batches. C# cache signatures include sorted `.cs`, shader and resource/scene sources; their namespace is separate from the previous GDScript caches. Cold generation and subsequent cache restoration were exercised without storing live characters or application state.

Evidence for this migration is under `/tmp/cozysora-csharp`: `baseline-captures`, `final-captures`, `performance-final`, `seabreeze-play.log` (the complete three-map session), and the final editor import/build logs. The README's capture commands reproduce the images. Physical gamepad hardware, genuine simultaneous multi-touch and exported builds remain outside this native macOS verification. Metal GPU timing reports zero and is treated as unavailable.


The launch script also succeeded from a fresh copied checkout with no `.godot`, `bin` or `obj` directory: package restore, C# build, project import and headless selector launch all completed without errors. This checks that local editor caches are not required to launch a new checkout.

Measured with Godot 4.7.2, Forward+ / Metal, Apple M3 Max, 1280 × 720, the unchanged 60 FPS cap and VSync enabled. Values below are individual warm entries, with generation-cache restoration confirmed in each log; they exclude .NET compilation and editor import.

| Map | GDScript warm load | C# warm load |
| --- | --- | --- |
| Seabreeze Village | 10.157 s | 8.536 s |
| Harbor Hills | 1.036 s | 1.073 s |
| Daan Gardens | 0.718 s | 0.847 s |

Seabreeze cold construction was observed at 31.781 s in GDScript and 16.413 s in C#. Its subsequent active gameplay frame windows had GDScript medians of 16.653–16.676 ms (p95 17.229–17.340 ms), compared with C# medians of 16.673–16.681 ms (p95 17.020–17.090 ms). Both retained the 60 FPS cadence at this pose. These observations suggest a construction benefit for Seabreeze in these runs; they do not demonstrate a general rendering speedup.

Another independently running game repeatedly rendered and took application focus during parts of the measurement session. Cozy Sora correctly auto-paused on focus loss. Paused samples were excluded from the active-play comparison above; the other maps' paused rendering samples are retained in the raw logs, not presented as gameplay benchmarks. The warm-load differences and cold-build ratio are therefore workstation observations under variable contention, not controlled benchmark claims. No renderer or gameplay behavior was changed to obtain the measurements.


A focused final-repository Daan rooftop check staged the ordinary player at the second café roof. The cat settled at Y = 16.075 m; paused-menu switching produced the gull above that roof and returned the cat to Y = 16.060 m, preserving supported height while physics remained frozen. The native process exited cleanly. This separately checks the translated surface queries and pause-time collision-body policy against the final scene/resource wiring.


The final native Harbor scenic run completed the entire 160-second cable-car circuit and continued into a second circuit. Route samples reached the hilltop at Z = 96 with reversed heading at 80 seconds, returned through Z = 66.799, 30.950 and −29, and reached the waterfront at Z = −100 with the original heading at 160 seconds. Bell triggers continued through both turnarounds. This run used the existing scenic mode to keep the camera fixed; character input and the shared player synthesizer were disabled, while map simulation and the spatial bell remained active. It exited with code 0 and no errors. Active rendering varied from approximately 43–60 FPS during the extended run; no general 60 FPS guarantee is claimed.

A second independent reviewer accepted the inspected migration code and native evidence for session ownership, pause handling, camera/audio cleanup, supported character switching, clean builds/import and fresh-checkout launch. The primary agent separately confirmed the complete transit circuit after that review. Both reviewers' acceptance is limited to the documented native scope, with the platform and measurement limitations retained above.
